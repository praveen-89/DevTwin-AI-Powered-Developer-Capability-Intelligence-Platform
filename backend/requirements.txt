# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
asyncpg==0.29.0
SQLAlchemy[asyncio]==2.0.35

# Configuration & Validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# JWT / Auth
PyJWT[crypto]==2.13.0   # Supabase JWKS asymmetric JWT verification (ADR-014)

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Dev tools (install separately if needed):
#   pip install ruff mypy
